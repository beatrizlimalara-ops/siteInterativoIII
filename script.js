/**
 * Alterna a classe '.flipped' no elemento do flashcard para disparar a animação 3D em CSS.
 * @param {HTMLElement} cardElement - O elemento .flashcard que recebeu o clique.
 */
function flipCard(cardElement) {
    cardElement.classList.toggle('flipped');
}
